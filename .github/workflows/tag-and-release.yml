name: Tag and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g. v1.0.0) — must match the plugin header Version.'
        required: true

permissions:
  contents: write

jobs:
  tag_and_release:
    name: Tag and Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout release branch
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: release
          # Full history and tags, so the tag-exists check below can see them.
          fetch-depth: 0

      - name: Abort if tag already exists
        env:
          VERSION: ${{ inputs.version }}
        run: |
          if git rev-parse -q --verify "refs/tags/${VERSION}" >/dev/null; then
            echo "::error::Tag ${VERSION} already exists."
            exit 1
          fi

      - name: Verify plugin header matches tag
        env:
          VERSION: ${{ inputs.version }}
        run: |
          header="$( sed -n 's/^ \* Version: *//p' site-icon-fallback.php | tr -d '[:space:]' )"
          if [ "${VERSION#v}" != "${header}" ]; then
            echo "::error::Tag ${VERSION} does not match plugin header Version: ${header}"
            exit 1
          fi

      - name: Resolve release commit
        id: commit
        run: echo "sha=$( git rev-parse HEAD )" >> "$GITHUB_OUTPUT"

      # GitHub's own source archive extracts to a version-named directory
      # (site-icon-fallback-1.0.0/), which becomes the plugin directory on a manual
      # wp-admin zip upload and so changes on every release. --prefix pins it.
      - name: Build plugin zip
        run: |
          git archive --format=zip \
            --prefix=site-icon-fallback/ \
            --output="${RUNNER_TEMP}/site-icon-fallback.zip" \
            HEAD

      # The release branch is stripped to the distributable tree before it is tagged, so
      # a zip missing the plugin's entrypoint means that strip took too much.
      - name: Verify zip is installable
        run: |
          if ! unzip -l "${RUNNER_TEMP}/site-icon-fallback.zip" \
            | grep -q 'site-icon-fallback/site-icon-fallback\.php'; then
            echo "::error::Built zip does not contain site-icon-fallback/site-icon-fallback.php"
            unzip -l "${RUNNER_TEMP}/site-icon-fallback.zip"
            exit 1
          fi

      - name: Create tag and GitHub Release
        uses: ncipollo/release-action@339a81892b84b4eeb0f6e744e4574d79d0d9b8dd # v1.21.0
        with:
          tag: ${{ inputs.version }}
          commit: ${{ steps.commit.outputs.sha }}
          name: ${{ inputs.version }}
          artifacts: ${{ runner.temp }}/site-icon-fallback.zip
          generateReleaseNotes: true
          draft: false
          prerelease: false
